---
import type { Service } from "../config/types";
interface Props { service: Service }
const { service } = Astro.props;
---
<article class="card">
  <h3><a href={`/services/${service.slug}/`}>{service.name}</a></h3>
  <p>{service.answer.split(". ")[0]}.</p>
</article>
