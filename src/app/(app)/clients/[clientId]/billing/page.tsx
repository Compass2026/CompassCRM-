import { PhaseStub } from "@/components/phase-stub";

export default function BillingPage() {
  return (
    <PhaseStub
      title="Billing"
      phase="Phase 3"
      description="Stripe subscription card, payment history, and webhook-driven paid / processing / open / past-due status — ported from the Show Me Electrical CRM."
    />
  );
}
