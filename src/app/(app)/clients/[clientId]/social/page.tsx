import { PhaseStub } from "@/components/phase-stub";

export default function SocialPage() {
  return (
    <PhaseStub
      title="Social tracker"
      phase="Phase 2"
      description="Log-only post tracker with calendar view and posts-per-month counter against the plan. In-app publishing (Meta Graph API) lands in Phase 4."
    />
  );
}
