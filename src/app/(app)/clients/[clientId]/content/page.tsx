import { PhaseStub } from "@/components/phase-stub";

export default function ContentPage() {
  return (
    <PhaseStub
      title="Content tracker"
      phase="Phase 2"
      description="Blog tracker — idea → brief → draft → review → published, linked to target keywords; monthly count feeds the Reporting cycle."
    />
  );
}
