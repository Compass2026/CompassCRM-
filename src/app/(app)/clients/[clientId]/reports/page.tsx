import { PhaseStub } from "@/components/phase-stub";

export default function ReportsPage() {
  return (
    <PhaseStub
      title="Reports"
      phase="Phase 2"
      description="One card per monthly cycle: report Drive link, rank summary, tasks completed. Cycles are created automatically on the 1st once the client converges to Reporting."
    />
  );
}
