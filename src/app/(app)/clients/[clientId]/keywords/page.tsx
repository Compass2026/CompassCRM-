import { PhaseStub } from "@/components/phase-stub";

export default function KeywordsPage() {
  return (
    <PhaseStub
      title="Keywords & Rank Tracker"
      phase="Phase 2"
      description="Target keyword set, GSC sync, BrightLocal per-city ranks and geo-grid, City Index, CSV import."
    />
  );
}
